# These files are read inside Linux containers (shell interpreters and Pinot's
# properties parser). A CRLF checkout on Windows would otherwise ship \r into
# the image - hence the sed 's/\r$//' in Dockerfile.apache-pinot.
*.sh   text eol=lf
*.conf text eol=lf
