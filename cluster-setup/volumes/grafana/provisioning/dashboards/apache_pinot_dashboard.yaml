apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /var/lib/grafana/dashboards
    disableDeletion: true   # Set to true to prevent deletion of dashboards not in this folder
    updateIntervalSeconds: 10